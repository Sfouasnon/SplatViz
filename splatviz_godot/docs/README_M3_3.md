# SplatViz M3.3

M3.3 moves the Msplat smoke-test terminal into a separate Godot window so the stage viewport remains visible and unobstructed.

New behavior:
- Top `Msplat` button opens a separate terminal window.
- Msplat controls are no longer a bottom overlay on the stage.
- Terminal is larger and wraps as a readable console.
- Browse Dataset Folder can select either a SplatViz Msplat dataset root or a clean images folder.
- Load Latest Splat still brings `splat.ply` back onto the active stage.

Msplat expected path:
`/Users/sfouasnon/msplat-env/bin/msplat-train`
